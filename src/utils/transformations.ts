import { PhoneNumberInput } from "../FunctionalApp/FunctionalForm";

export const capitalize = () => {
  // todo: build this function
  // `capitalize("jOn")` should output `"Jon"`
};

export const formatPhoneNumber = (arr: PhoneNumberInput) => arr.join("-");
